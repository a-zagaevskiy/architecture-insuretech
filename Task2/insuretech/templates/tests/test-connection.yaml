apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "insuretech.fullname" . }}-test-connection"
  labels:
    {{- include "insuretech.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "insuretech.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
