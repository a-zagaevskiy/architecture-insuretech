apiVersion: v1
kind: Service
metadata:
  name: {{ include "insuretech.fullname" . }}
  labels:
    {{- include "insuretech.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "insuretech.selectorLabels" . | nindent 4 }}
